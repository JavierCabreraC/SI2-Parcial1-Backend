{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "python -m venv && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn pos_backend.wsgi:application",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
